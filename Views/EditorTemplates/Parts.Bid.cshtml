@model Devq.Bids.Models.BidPart

@{
}

@Html.Hidden("Name", WorkContext.CurrentUser.UserName ?? "")

<fieldset class="what">
    @Html.LabelFor(m => m.BidPrice, T("Bid"))
    @Html.TextBoxFor(m => m.BidPrice, new { @class = "bid-text", id = "bidform-currency" })
    @Html.HiddenFor(m => m.BidedOn)
</fieldset>

@using (Script.Foot())
{
    <script>
        //$(function () {
        //    $("#bidform-currency").maskMoney({prefix: '&euro;'});
        //});
    </script>
}